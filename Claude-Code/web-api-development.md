Use the following prompt to direct claude code to develop your application:
```
Using the project requirements defined in CLAUDE.md, build a complete FastAPI application.

The application should include:

• GET /tasks
• GET /tasks/{id}
• POST /tasks
• PUT /tasks/{id}
• DELETE /tasks/{id}

Store tasks in an in-memory list.

Each task should contain:

• id
• title
• description
• completed

Generate clean, modular, well-documented code.
```